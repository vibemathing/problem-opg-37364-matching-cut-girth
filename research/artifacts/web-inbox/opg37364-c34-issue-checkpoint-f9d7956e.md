## C34 canonical classification — NONTERMINAL_CHECKPOINT / candidate_only

Fresh-read base: `f9d7956eda5ac0f30d22f0fb8e48de3d3bc1870a`. Existing Issue #3 and
C31–C33 reused; no frozen source, truth record or admission_request changed.

Native Lean compilation did NOT occur. The exact source environment has
Lean commit819816b2e0a3bf405af45ae5c7af2491d8f5bee6 and Mathlib
0df444a360eaa60ab8c11dca51a86af692955474. Current tools did not provide that
executable runtime. The official matching Linux build artifact was found;
its download was rejected at the documented tool size cap. No cap bypass,
ENOENT loop, compiler rejection or axiom output is claimed.

New progress: explicit invertible T/Q/L/R canonical encoding; proof that the
four NONZERO classes apply only after compatibility, with a concrete invalid
raw-witness control; successor Lean source constructs the Encodes table from
realization rather than assuming it, and connects output conflict to C32's
b=1,k=2 zero count. A separate isolated direct projection draft removes the
irrelevant C32 dependency of bad22_le_incidence itself. All Lean is uncompiled.
The actual Witness/index Lean Equiv remains the first missing translation.

Actual Python atlas covers every colour choice in the fixed24 palette and
all endpoint-equality types: T6072,Q31878,L63756,R63756 compatible local codes;
L/R each12420 conflicts. Both inverse directions are checked. Runtime record:
CPython3.13.5, exit0,4.5275s; wall40s,CPU35s,256MiB,64KiB per stream.
This is finite atlas validation, NOT a Lean compilation or trusted proof.
The arbitrary-N support-renaming proof is separate from the finite replay.

The only retained bound is the C33 class contribution
Pr(Bad22)<=69828/N-31878/N^2 for N>=1. No extra addition to the whole coarse
catalogue bound and no total<41/800 or root-family theorem.

best_verified_candidate=none; best_verified_result=none; trusted_verified_scope=[].
Both root and contract-edge-case obligations remain open. Shared-edge,
shared-path, vertex-only mixed pairs and disjoint deletion families remain.

Current newly prepared files are LOCAL ONLY; no branch/commit/PR/check/merge
or posted checkpoint is claimed. This body is an unposted recovery artifact.
See the checkpoint.json and manifest for exact file digests and native gaps.
